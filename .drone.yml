kind: pipeline
type: docker
name: default

steps:
- name: submodules
  image: alpine/git
  commands:
    - git submodule update --recursive --init

- name: build
  image: kitware/cmake
  commands:
    - cmake -E make_directory ./build
    - cd ./build
    - cmake -S .. -B .
    - make
