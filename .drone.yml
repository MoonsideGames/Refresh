kind: pipeline
type: docker
name: default

steps:
- name: submodules
  image: fedora
  commands:
    - git submodule update --recursive --init

- name: build
  image: fedora
  commands:
    - dnf install cmake SDL2-devel
    - cmake -E make_directory ./build
    - cd ./build
    - cmake -S .. -B .
    - make
